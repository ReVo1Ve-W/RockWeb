# 复制这个文件为 .env.local，本地开发时 Vite 会自动读取
# .env.local 不会被提交到 git（Vite 项目默认的 .gitignore 已包含这条规则）
#
# 本地开发指向本地后端；部署到 Vercel 后，去 Vercel 项目设置里配置同名环境变量，
# 值改成 Render 后端的线上地址（例如 https://rockweb-backend.onrender.com/api）
VITE_API_BASE_URL=http://localhost:4000/api
